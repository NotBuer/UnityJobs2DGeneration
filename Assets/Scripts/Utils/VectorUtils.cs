using Unity.Collections;
using UnityEngine;

public static class VectorUtils
{

    //public static Vector3[] ConvertArrayVector3IntToVector3(ref NativeSlice<Vector3Int> nativeSliceArray)
    //{
    //    Vector3[] array = new Vector3[nativeSliceArray.Length];
    //    for (int i = 0; i < array.Length; i++)
    //    {
    //        array[i] = new Vector3(nativeSliceArray[i].x, nativeSliceArray[i].y);
    //    }
    //    return array;
    //}

}
